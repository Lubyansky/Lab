/*
#include <stdio.h>
#include <math.h>
#include <locale.h>


int main(void)
{
	setlocale(LC_ALL, "RUS");
	
	
	float a, b, x, y, z;


	printf("x = "); 
	scanf_s("%f", &x);
	printf("y = "); 
	scanf_s("%f", &y);
	printf("z = "); 
	scanf_s("%f", &z);
	

	a = (y++) * (x + (y / (pow(x, 2) + 4))) / (exp(-x - 2) + (1 / (pow(x, 2) + 4)));

	b = (1 + cos(y - 2)) / (pow(x, 4)/2 + pow(sin(z), 2));
	
	
	printf("a = %f", a);
	printf("\nb = %f", b);
	
	
	return(0);
}
*/
