/*
#include <stdio.h>
#include <math.h>
#include <locale.h>


int main(void)
{
		int n;
	float sum = 0, k = 0;
	
	
	printf("n = ");
	scanf_s("%d", &n);
	
	
	if(n < 1) 
	{
		printf("The operation cannot be performed");
		return(0);
	}


	for(int i = 1; i <= n; i++)
	{

		sum = 1 / (sin(i) + k) + sum; 
		k = sin(i) + k;

	}
	
	
	printf("sum = %f", sum);
}
*/
